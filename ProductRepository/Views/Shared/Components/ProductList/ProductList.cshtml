@model List<ProductListComponentViewModel>

<table class="table table-hover">
	<thead>
		<tr>
			<th>Color</th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>@item.Color</td>
			</tr>
		}
	</tbody>
</table>